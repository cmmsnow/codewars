public class ConsecutiveStrings {

    //Return the first longest string consisting of k consecutive strings taken in the array.
    public String concatLongest(String[] array, Integer numOfStrings){
        String firstLongest = "";
        //concat number of strings together, via for loop
        //count length and save longest string to variable if greater than length of current saved
        //return longest string

        return firstLongest;
    }
}
